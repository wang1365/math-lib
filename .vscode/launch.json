{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node-terminal",
      "request": "launch",
      "name": "启动：npm run dev",
      "command": "npm run dev",
      "cwd": "${workspaceFolder}"
    }
  ]
}
